/**
 * Shared chart configuration for consistent, detailed charts across the app.
 */

/** Standard sparkline dimensions - used in TopCryptoWidget, Portfolio, etc. */
export const SPARKLINE_WIDTH = 160;
export const SPARKLINE_HEIGHT = 56;

/** Days of data for sparklines (7d for more detail) */
export const SPARKLINE_DAYS = 7;
